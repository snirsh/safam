# Database (Neon Postgres — auto-set by Vercel Marketplace integration)
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Encryption (generate: openssl rand -hex 32)
ENCRYPTION_KEY=

# Webhook auth between GitHub Actions scraper and API
WEBHOOK_API_KEY=

# WebAuthn / Passkeys
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Safam
WEBAUTHN_ORIGIN=http://localhost:3000

# Google Gemini (for transaction categorization)
GOOGLE_GENERATIVE_AI_API_KEY=

# JWT session signing (generate: openssl rand -hex 32)
JWT_SECRET=
