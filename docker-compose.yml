services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: safam
      POSTGRES_PASSWORD: safam
      POSTGRES_DB: safam
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
